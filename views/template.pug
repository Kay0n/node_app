
head
	title Database Design
	link(rel="stylesheet", href="/style.css", type='text/css')
body
	- var menu = { 'Home': '/list', 'Input': '/input', 'Upload': '/upload', 'Graph': '/graph'};
	block page
		- var selected = "404"
	
	// top navigation bar
	nav(class="topnav")
		each val, key in menu	
			- var lowerKey = key.toLowerCase()
			if selected == lowerKey
				a(href=val, title=key, class="selected")= key
			else
				a(href=val, title=key)= key
	br
	block content_var

	br
	
	footer 
		hr
		p &copy; JM 2021		
